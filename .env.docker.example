# Docker 환경용 설정 예시
# 이 파일을 .env.docker로 복사하고 실제 값을 입력하세요
# cp .env.docker.example .env.docker

# Application
APP_NAME=Docent AI Core API
APP_VERSION=0.1.0
DEBUG=false

# Server
HOST=0.0.0.0
PORT=8080

# Database (SQLite) - Docker 내부 경로
DATABASE_PATH=/app/data/docent.db
DATABASE_ECHO=false
DATABASE_POOL_SIZE=5
DATABASE_MAX_OVERFLOW=10

# Image Upload - Docker 내부 경로
IMAGE_UPLOAD_PATH=/app/data/uploads

# Google Cloud Storage (Optional - for image storage)
GCS_PROJECT_ID=your-project-id
# GCS_CREDENTIALS_PATH=/app/credentials/gcs-key.json

# Gemini API
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL_NAME=gemini-3-flash-preview

# Google Custom Search API (for video reference collection)
GOOGLE_SEARCH_API_KEY=your-google-search-api-key-here
GOOGLE_SEARCH_ENGINE_ID=your-search-engine-id-here
GOOGLE_SEARCH_API_URL=https://www.googleapis.com/customsearch/v1

# JWT Authentication
JWT_SECRET_KEY=your-secret-key-change-this-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_DAYS=7

# Logging
LOG_LEVEL=INFO

# Environment
ENV=production
